enable-pre-post-scripts=true

# ── Security ─────────────────────────────────────────────────────
# Isolated node_modules: packages can only access explicitly declared deps
node-linker=isolated
# Fail on unmet peer dependencies
strict-peer-dependencies=true
# Auto-install missing peer dependencies
auto-install-peers=true
# Prevent silent lockfile modifications
prefer-frozen-lockfile=true
# Pin exact versions in package.json (no ^ or ~)
save-exact=true
# Disable side-effects cache (prevents potential cache poisoning)
side-effects-cache=false
